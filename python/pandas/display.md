# Pandas Display Options

```python
pd.options.display.max_rows = 200     # (default=50)
pd.options.display.max_columns = 100  # (default=50)

pd.options.display.max_colwidth = 50  # (default=50)

pd.options.display.precision = 6      # number of decimal points (default=6)
```
